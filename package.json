{
  "name": "campfire-scene",
  "version": "1.0.0",
  "description": "A campfire camping scene game with Electron, A-Frame, and Three.js",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"cd client && npm run dev\" \"wait-on http://localhost:5173 && electron .\"",
    "build": "cd client && npm run build",
    "dist": "npm run build && electron-builder"
  },
  "devDependencies": {
    "electron": "latest",
    "electron-builder": "latest",
    "concurrently": "^8.2.1",
    "wait-on": "^7.0.1"
  }
}
